apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-mysql
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  mysql-dashboard.json: |
    {
      "id": null,
      "title": "MySQL Overview",
      "tags": ["mysql"],
      "timezone": "browser",
      "schemaVersion": 16,
      "version": 1,
      "refresh": "5s",
      "panels": []
    }
